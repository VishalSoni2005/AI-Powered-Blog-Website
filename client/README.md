Total context create in project are : (for this project we have mostly provided states to context)

1> User context
    values: { userAuth, setUserAuth }

2> Editor context
    values : { blog, setBlog, editorState, setEditorState, textEditor, setTextEditor }

3> Blog context
    values: { blog, setBlog }